# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['installer/voice/voice_agent.py'],
    pathex=['src', 'installer/voice'],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)

pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='YOMA-Voice-Agent',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    contents_directory='voice_internal',
    version='release/work/yoma-version.txt',
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)

coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    name='YOMA-Voice-Agent',
    strip=False,
    upx=True,
    upx_exclude=[],
)
